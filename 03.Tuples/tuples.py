# Tuple type 선언하기

# { }는 set라고 한다.. 
# a = {3, 45, 8}  

# tuple은 ( ), 괄호 사용한다. 
a = (3, 4)
b = [3, 4, 55]

print(type(a))
print(a)

# immutable 하다
# a.

# b같은 list에서 지원하는 append, remove 등 다양한 메소드들을 지원하지 않는다.
# b.